---
import React from 'react';
import Router from '../components/Router';

const Page: React.FC = () => {
  return <Router />;
};

export default Page;
---

<React.StrictMode>
  <Page client:load />
</React.StrictMode>